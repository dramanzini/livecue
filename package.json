{
  "name": "livecue",
  "version": "0.1.0",
  "private": true,
  "description": "Setlist controller for Ableton Live — read locators, build a setlist, control transport and show synced lyrics from any browser on your network. An open-source take on AbleSet.",
  "license": "MIT",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm -w server run dev",
    "dev:client": "npm -w client run dev",
    "build": "npm -w client run build",
    "start": "npm -w server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
